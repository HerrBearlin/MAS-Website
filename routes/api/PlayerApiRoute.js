/*
const express = require('express');
const router = express.Router();

const pApiController = require('../../api/PlayerApi');

router.get('/', pApiController.getPlayers);
router.get('/:pId', pApiController.getPlayerById);
router.post('/', pApiController.createPlayer);
router.put('/:pId', pApiController.updatePlayer);
router.delete('/:pId', pApiController.deletePlayer);

module.exports = router;*/
