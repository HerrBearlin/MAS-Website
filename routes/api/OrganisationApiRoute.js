/*
const express = require('express');
const router = express.Router();

const orgApiController = require('../../api/OrganisationApi');

router.get('/', orgApiController.getOrganisations);
router.get('/:orgId', orgApiController.getOrganisationById);
router.post('/', orgApiController.createOrganisation);
router.put('/:orgId', orgApiController.updateOrganisation);
router.delete('/:orgId', orgApiController.deleteOrganisation);

console.log("Organisation router used.");

module.exports = router;*/
